---
import { getCollection, type CollectionEntry } from "astro:content";
import ArticleLayout from "../../../../layouts/ArticleLayout.astro";
import { findTranslation, getNotePath, getNoteSlug } from "../../../../lib/notes";

export async function getStaticPaths() {
  const notes = await getCollection("notes", ({ data }) => !data.draft);
  const sections = { books: "livros", articles: "artigos" } as const;
  return notes
    .filter((note) => note.data.language === "pt-BR" && (note.data.category === "books" || note.data.category === "articles"))
    .map((note) => {
      const translation = findTranslation(note, notes);
      return {
        params: { section: sections[note.data.category as "books" | "articles"], slug: getNoteSlug(note) },
        props: { note, alternatePath: translation ? getNotePath(translation) : `/reading/${note.data.category}/` },
      };
    });
}

interface Props { note: CollectionEntry<"notes">; alternatePath: string; }
const { note, alternatePath } = Astro.props;
---

<ArticleLayout note={note} alternatePath={alternatePath} />
